{
  "name": "@ticketingjuant/common",
  "version": "1.0.5",
  "description": "",
  "main": "./build/index.js",
  "types": "./build/index.d.ts",
  "files": [
    "build/**/*"
  ],
  "scripts": {
    "build": "del ./build/* && tsc",
    "clean": "del ./build/*",
    "pub": "git add . && git commit -m \"publish\" && git push && npm version patch && del ./build/* && tsc && npm publish"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.1.0",
    "typescript": "^5.3.3"
  }
}
